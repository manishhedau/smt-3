const SectionTile = (props) => {

    const {id,heading, text} = props;

    return (
        <div className="section-tile" id={id}>
            <div className="description">

            </div>
        </div>
    );
}